#include <stdio.h>
int main() {
    int num = 40;
    if((num>=-10) && (num <=40)){
        printf ("%d is between -10 and 40 degrees\n", num);
    }else{
        printf ("%d is not between -10 and 40 degrees\n", num);
    }

}